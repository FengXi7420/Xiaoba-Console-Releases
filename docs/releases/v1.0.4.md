# 小吧控制台 v1.0.4

发布日期：2026-08-18

本 hotfix 修复部分 Intel 平台首次读取 IPF/DTT 时超时，导致已满足签名身份条件的定制系统不显示 EWP 配置的问题。

## 主要更新

- 将 IPF 遥测 helper 的默认首次采样预算从 3 秒提高到 10 秒。现场证据显示，Intel Core Ultra 9 290HX Plus 的首次初始化约需 6.3 秒；原 3 秒预算会在有效结果返回前终止读取。
- 发布脚本会在打包前从最终 `MControl.exe` 读取 `MControl.CustomSystemIdentityPublicKey.xml`，并与源码公钥按 `Trim()` 后的 Ordinal 比较。嵌入资源与源码不一致时，发布会直接失败。
- 保持原有 Intel、DTT 可读性和签名身份门，不因本次修复放宽 EWP 的能力判定或身份验证要求。

## 正式产物

| 文件 | 大小 | SHA-256 |
| --- | ---: | --- |
| `MControl_v1.0.4_Setup.exe` | 16,853,358 bytes | `E924D888DB46C6F8F174FB2017E3E390BC9C6F415EFCE925ADF5C46A0C54A0C5` |
| `MControl_v1.0.4_Setup_WebView2_Fixed.exe` | 224,108,235 bytes | `E89DFB6171C13859728574EE5150FB52BA811C96277BCB64FC4D16CB5F0D2BC8` |
| `MControl_v1.0.4_Portable.zip` | 16,634,904 bytes | `AD84BF95136E51DFFE48926A93194E1FFDA337D839E6C5D2A5EABF5023B1DE53` |

`SHA256SUMS.txt` 同时作为 Release 附件提供。Fixed 安装版继续内嵌微软官方 x64 WebView2 Fixed Version Runtime。

## 验证

- Node 静态与行为测试：145 passed / 0 failed。
- xUnit：485 passed / 0 failed。
- Release build：0 warning / 0 error。
- 最终 `MControl.exe` 的嵌入身份公钥已与源码公钥完成归一化逐字节校验。
- 正常、Fixed WebView2 和便携发布产物均已生成并用 `SHA256SUMS.txt` 复核。

## 升级提示

- 可直接运行安装包覆盖升级；小吧控制台与 M-Control 共享技术安装身份，不能并存。
- 已安装 Fixed WebView2 版本的用户应继续选择 `Setup_WebView2_Fixed.exe`。
- 安装新版后，请彻底退出旧版小吧控制台及托盘进程再重新启动，以便重新采集 EWP 状态并清除进程内身份缓存。
- 原试验机仍需用本版本完成一次 EWP 显示复测；本次发布不把单机证据扩展为多机型兼容性结论。
